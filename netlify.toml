# Netlify configuration. Cloudflare Pages uses the same build command / output
# directory (set them in the Pages dashboard) and reads dist/_headers identically.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
